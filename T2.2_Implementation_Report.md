# T2.2 Implementation Report

**Ticket:** T2.2 — Implement AssetLoader  
**Phase:** 2 — Engine Input / Audio / Utils  
**Date:** 2026-06-21  
**Status:** COMPLETE

## Files Created

| File | Description |
|---|---|
| `src/engine/utils/asset_loader.py` | `AssetLoader` classmethod cache + `SpriteSheet` helper |

## Files Modified

| File | Change |
|---|---|
| `tests/test_asset_loader.py` | New Phase 2 test file covering AssetLoader caching and SpriteSheet |

## Acceptance Criteria Verification

| Criterion | Status | Evidence |
|---|---|---|
| `AssetLoader` caches by `str(path)` | ✅ | Two loads of same path return identical object (`is` check in tests) |
| `load_image` returns same object on repeat load | ✅ | `test_load_image_caches` passes |
| Different paths return different objects | ✅ | `test_load_image_different_paths_different_objects` passes |
| `SpriteSheet` frame count matches dimensions | ✅ | `test_load_spritesheet_frame_count` passes |
| Missing file raises `FileNotFoundError` | ✅ | `test_load_missing_file_raises` passes |
| Flake8 clean | ✅ | `flake8 src/engine/utils/asset_loader.py tests/test_asset_loader.py --max-line-length=79` — no errors |
| No regressions in existing tests | ✅ | `pytest tests/ -v` — 25/25 passing |

## Implementation Notes

- Per `22_API_CONTRACTS.md` §5.2 the cache key is the literal `str(path)` — no path rewriting or `ASSETS_DIR` prepending.
- `load_image` defers `convert()`/`convert_alpha()` because `pygame.display` is not initialized during unit tests; format conversion is the caller's responsibility once display is ready.